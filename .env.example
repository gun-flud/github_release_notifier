# Server
PORT=####

# Database
DB_HOST=localhost
DB_PORT=####
DB_NAME=github_notifier
DB_USER=postgres
DB_PASSWORD=password

# Email (SMTP)
SMTP_HOST=localhost
SMTP_PORT=####
SMTP_USER=user
SMTP_PASS=password
EMAIL_FROM=noreply@github-notifier.com

# GitHub
GITHUB_TOKEN=               # (5000 req/hr vs 60)

# App
BASE_URL=  # Used to build confirmation links in emails

# Scanner
SCAN_INTERVAL_MINUTES=    # How often to check for new releases